type EntireElement = OneOrMore<JSX.Element | string | number | boolean | null | undefined>;

type ExistElement = OneOrMore<JSX.Element | string | number | boolean>;

interface SvgIconProps {
  className?: string;
  style?: React.CSSProperties;
}
